/*
 * package in.practice.BookHibernate.Util;
 * 
 * import org.hibernate.SessionFactory;
 * 
 * 
 * public class HibernateUtil { private static SessionFactory sessionFactory;
 * static { AnnotationConfiguration config = new AnnotationConfiguration(); //
 * By default hibernate.cfg.xml config.configure();
 * 
 * //Session for JAVA and hibernate communication sessionFactory =
 * config.buildSessionFactory(); } public static SessionFactory
 * getSessionFactory(){ return sessionFactory; } }
 * 
 */